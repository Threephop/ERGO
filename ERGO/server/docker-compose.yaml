version: '3'

services:
  ergo:
    image: ergo:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "python -m uvicorn main:app --reload --port=8000 --host=0.0.0.0"
    ports:
      - "8000:8000"